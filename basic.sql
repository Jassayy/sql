-- the where clause
SELECT *
FROM customers
-- WHERE points > 3000;
-- WHERE state <> 'VA'; -- put string in ''
-- WHERE state != 'VA';
-- WHERE state = 'VA';
WHERE birth_date = '2004-02-19';

-- operators we can use in sql
-- >
-- <=
-- <
-- >=
-- =
-- != or <> we can use both of these for not equal